# Terraform Configuration for GCP VMs

This Terraform configuration creates 2 GCP VMs with 2vCPU and 8GB RAM.

## Prerequisites

- Google Cloud account
- Terraform installed
- gcloud CLI installed

## Usage

1. Initialize the Terraform configuration: `terraform init`
2. Apply the Terraform configuration: `terraform apply`
3. Destroy the resources: `terraform destroy`
